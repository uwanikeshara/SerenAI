## stress_model.tflite — PLACEHOLDER

This file must be replaced with your trained model.

### How to get the real model:
1. Open `d:\Projects\Uwani NEW\Project\ml\`
2. Double-click `RUN_TRAINING.bat`
3. Follow the on-screen instructions
4. After it finishes, copy:
   - `ml\models\stress_model.tflite` → this folder
   - `ml\models\labels.txt` → (already here, no change needed)

The app will show a "model not found" error until this file is present.
